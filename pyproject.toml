[project]
name = "currency-converter-py"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "customtkinter>=5.2.2",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
